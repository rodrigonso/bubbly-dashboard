export default class Vehicle {
  constructor(data) {
    // this.id = data.id;
    this.make = data.make;
    this.model = data.model;
    this.name = data.name;
    this.type = data.type;
    this.icon = data.icon;
  }
}
